spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spotifimini_db
    username: ohgiraffers
    password: ohgiraffers

# mybatis config
mybatis:
  mapper-locations: mappers/**/*.xml